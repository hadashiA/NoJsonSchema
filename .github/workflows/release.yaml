name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "tag: git tag you want create. (sample 1.0.0)"
        required: true
      dry-run:
        description: "dry-run: false = create release/nuget. true = never create release/nuget."
        required: true
        default: false
        type: boolean

env:
  GIT_TAG: ${{ github.event.inputs.tag }}

jobs:
  build-dotnet:
    permissions:
      contents: read    # actions/checkout — explicitly granted because setting any permission
                        # at job scope clears the workflow defaults (incl. the implicit contents: read).
      id-token: write   # OIDC token for NuGet/login
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.0.x

      - run: dotnet build -c Release -p:Version=${{ env.GIT_TAG }}
      - run: dotnet test -c Release --no-build
        env:
          TZ: Asia/Tokyo

      - run: dotnet pack -c Release --no-build -o ./publish -p:Version=${{ env.GIT_TAG }}

      - uses: actions/upload-artifact@v4
        with:
          name: nuget
          path: ./publish

      - name: Login to NuGet
        if: github.event.inputs.dry-run == 'false'
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USERNAME }}

      - name: Push to NuGet
        if: github.event.inputs.dry-run == 'false'
        run: |
          dotnet nuget push ./publish/*.nupkg \
            --api-key ${{ steps.login.outputs.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate

  create-release:
    if: github.event.inputs.dry-run == 'false'
    needs: [build-dotnet]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 10
    steps:
      - uses: softprops/action-gh-release@v1
        id: create_release
        with:
          tag_name: ${{ env.GIT_TAG }}
          name: v${{ env.GIT_TAG }}
          draft: true
          prerelease: false
          generate_release_notes: true
