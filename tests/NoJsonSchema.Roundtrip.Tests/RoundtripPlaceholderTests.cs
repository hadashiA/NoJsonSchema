using Xunit;

namespace NoJsonSchema.Roundtrip.Tests;

public class RoundtripPlaceholderTests
{
    [Fact]
    public void Placeholder()
    {
        // Real roundtrip tests land in M4: compile generated code in-memory and parse/serialize sample JSON.
        Assert.True(true);
    }
}
