using Xunit;

namespace NoJsonSchema.Snapshot.Tests;

public class SnapshotPlaceholderTests
{
    [Fact]
    public void Placeholder()
    {
        // Real snapshot tests land in M4 (minimal emitter).
        Assert.True(true);
    }
}
